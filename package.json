{
  "name": "regression-cube",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "node server/server.js",
    "postinstall": "cd client && ../node_modules/bower/bin/bower install;cd .. && mkdir uploads && mkdir tmp"
  },
  "dependencies": {
    "bower": "^1.3.12",
    "connect-multiparty": "^1.0.4",
    "express": "^4.3.1"
  }
}
